FROM nginx:latest
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
```

> This uses **nginx** as base image and copies your `index.html` into it.

---

Your repo should have **exactly 2 files:**
```
jenkins-docker-project/
├── index.html
├── Dockerfile
